node:
  id: "flowplane-test-envoy"
  cluster: "flowplane-test-cluster"

dynamic_resources:
  ads_config:
    api_type: DELTA_GRPC
    transport_api_version: V3
    grpc_services:
      - envoy_grpc:
          cluster_name: flowplane_xds_cluster
  cds_config:
    ads: {}
    resource_api_version: V3
  lds_config:
    ads: {}
    resource_api_version: V3

static_resources:
  clusters:
  - name: flowplane_xds_cluster
    type: LOGICAL_DNS
    connect_timeout: 5s
    lb_policy: ROUND_ROBIN
    http2_protocol_options: {}
    load_assignment:
      cluster_name: flowplane_xds_cluster
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: 127.0.0.1
                port_value: 18003
    transport_socket: # Remove or adjust for plaintext or different certificate layout
      name: envoy.transport_sockets.tls
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
        sni: flowplane-control-plane
        common_tls_context:
          tls_certificates:
            - certificate_chain:
                filename: /etc/envoy/certs/xds-client.pem
              private_key:
                filename: /etc/envoy/certs/xds-client.key
          validation_context:
            trusted_ca:
              filename: /etc/envoy/certs/xds-ca.pem
            match_subject_alt_names:
              - exact: flowplane-control-plane

admin:
  address:
    socket_address:
      address: 127.0.0.1
      port_value: 9901

layered_runtime:
  layers:
  - name: static_layer_0
    static_layer:
      envoy:
        logging:
          xds: debug
